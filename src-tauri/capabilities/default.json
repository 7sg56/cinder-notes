{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": ["main", "onboarding"],
  "permissions": [
    "core:default",
    "core:menu:default",
    "dialog:default",
    "fs:default"
  ]
}
